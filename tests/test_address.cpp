#include "address.h"

#include <catch2/catch_test_macros.hpp>
#include <catch2/matchers/catch_matchers.hpp>
#include <catch2/matchers/catch_matchers_string.hpp>

TEST_CASE("Constructors", "[init][address]") {
    SECTION("Default") {
        CHECK(false);
    }

    SECTION("value_type Constructor") {
        CHECK(false);
    }

    SECTION("Copy Constructor") {
        CHECK(false);
    }

    SECTION("Move Constructor") {
        CHECK(false);
    }
}

TEST_CASE("Assignment", "[init][address]") {
    SECTION("Copy Assignment") {
        CHECK(false);
    }
    SECTION("Move Assignment") {
        CHECK(false);
    }
}

TEST_CASE("Accessors", "[init][address]") {
    SECTION("Copy Assignment") {
        CHECK(false);
    }
    SECTION("Move Assignment") {
        CHECK(false);
    }
}